# System

- macOS (audio feedback uses the built-in `say` command)
- Webcam
- Python 3.8+

## Python Dependencies

Install the required packages with:

```bash
pip install opencv-python dlib numpy pillow
